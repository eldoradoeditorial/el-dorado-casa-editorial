---
import BaseLayout from '../layouts/BaseLayout.astro';
import Card from '../components/ui/Card.astro';
// import Button from '../components/ui/Button.astro'; // ❌ no se usa
import CallToAction from '../components/sections/CallToAction.astro';
import { siteData } from '../utils/data';
---

<BaseLayout 
  title={`Nosotros - ${siteData.name}`}
  description={`Conoce la historia, misión, visión y valores de ${siteData.name}. Descubre nuestro compromiso con la literatura de calidad y la excelencia editorial.`}
>
  <!-- Hero Section -->
  <section class="section bg-gradient-to-br from-bg-primary to-bg-secondary">
    <div class="container">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-hero text-gradient-primary mb-6">
          Nuestra Historia
        </h1>
        <p class="text-lead mb-8">
          Como el mítico El Dorado que inspiró expediciones en busca de tesoros, 
          nosotros exploramos el universo literario en busca de las mejores historias.
        </p>
      </div>
    </div>
  </section>

  <!-- Historia -->
  <section class="section">
    <div class="container">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-4xl font-display font-bold text-primary-dark mb-6">
            El origen de nuestro nombre
          </h2>
          <div class="prose prose-lg text-text-secondary space-y-4">
            <p>{siteData.story.origin}</p>
            <p>{siteData.story.inspiration}</p>
            <p>{siteData.story.philosophy}</p>
          </div>
        </div>
        <div class="bg-gradient-to-br from-gold/20 to-gold-light/20 rounded-2xl p-8 text-center">
          <div class="text-6xl mb-6">🏛️</div>
          <blockquote class="text-xl italic text-primary-dark font-medium">
            "Así como el mito ha trascendido generaciones y fronteras, 
            buscamos que nuestras publicaciones se conviertan en hallazgos valiosos 
            que perduren en la memoria de los lectores."
          </blockquote>
        </div>
      </div>
    </div>
  </section>

  <!-- Misión y Visión -->
  <section id="mision" class="section bg-bg-secondary">
    <div class="container">
      <div class="grid md:grid-cols-2 gap-12">
        <Card variant="elevated" padding="lg">
          <div class="text-center mb-6">
            <div class="w-16 h-16 bg-primary rounded-full flex items-center justify-center mx-auto mb-4">
              <span class="text-3xl text-white">🎯</span>
            </div>
            <h2 class="text-3xl font-display font-bold text-primary-dark">Misión</h2>
          </div>
          <p class="text-text-secondary text-lg leading-relaxed">
            {siteData.mission}
          </p>
        </Card>

        <Card variant="elevated" padding="lg">
          <div class="text-center mb-6">
            <div class="w-16 h-16 bg-gold rounded-full flex items-center justify-center mx-auto mb-4">
              <span class="text-3xl text-text-primary">🔮</span>
            </div>
            <h2 class="text-3xl font-display font-bold text-primary-dark">Visión</h2>
          </div>
          <p class="text-text-secondary text-lg leading-relaxed">
            {siteData.vision}
          </p>
        </Card>
      </div>
    </div>
  </section>

  <!-- Valores -->
  <section class="section">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-display font-bold text-primary-dark mb-4">
          Nuestros Valores
        </h2>
        <p class="text-xl text-text-secondary max-w-2xl mx-auto">
          Los principios que guían cada decisión y cada proyecto en {siteData.name}.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {siteData.values.map((valor) => (
          <Card hover={true}>
            <div class="text-center">
              <div class="text-4xl mb-4">{valor.icono}</div>
              <h3 class="text-xl font-display font-semibold text-primary-dark mb-3">
                {valor.titulo}
              </h3>
              <p class="text-text-secondary">
                {valor.descripcion}
              </p>
            </div>
          </Card>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <CallToAction
    title="¿Quieres formar parte de nuestra historia?"
    description="Ya sea como autor, colaborador o lector, hay un lugar para ti en El Dorado Servicios Editoriales."
    primaryButton={{
      text: "Enviar tu Manuscrito",
      href: "/enviar-manuscrito",
      variant: "gold"
    }}
    secondaryButton={{
      text: "Trabajar con Nosotros",
      href: "/contacto",
      variant: "ghost"
    }}
  />
</BaseLayout>
