---
import Header from "../components/layout/Header.astro";
import Footer from "../components/layout/Footer.astro";
import { siteData } from "../utils/data"; // 👈 única fuente de verdad

export interface Props {
  title: string;
  description?: string;
  image?: string;
  type?: "website" | "article" | "book";
  noindex?: boolean;
}

const {
  title,
  description = "El Dorado Servicios Editoriales - Donde las palabras son tesoros. Publicamos literatura de alta calidad y obras académicas de excelencia.",
  image = "/images/og-default.jpg",
  type = "website",
  noindex = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const socialImage = new URL(image, Astro.site);
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Basic Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    {
      noindex ? (
        <meta name="robots" content="noindex, nofollow" />
      ) : (
        <meta name="robots" content="index, follow" />
      )
    }
    <link rel="canonical" href={canonicalURL} />

    <!-- Favicon -->
    <link rel="icon" href="/images/logos/logo.svg" type="image/svg+xml" />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:site_name" content={siteData.name} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />

    <style is:global>
      @import "../styles/global.css";
    </style>
  </head>

  <body>
    <!-- Skip to main content for accessibility -->
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-accent text-white px-4 py-2 rounded-md z-50"
    >
      Saltar al contenido principal
    </a>

    <div class="min-h-screen flex flex-col">
      <!-- Header -->
      <Header site={siteData} />
      <!-- 👈 ahora Header recibe site -->

      <!-- Main Content -->
      <main id="main" class="flex-grow">
        <slot />
      </main>

      <!-- Footer -->
      <Footer site={siteData} />
      <!-- 👈 y Footer también -->
    </div>

    <!-- Alpine: cargar como módulo desde archivo propio -->
    <script type="module" src="/scripts/alpine.js"></script>
  </body>
</html>
